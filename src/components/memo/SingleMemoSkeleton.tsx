import { Skeleton as SK } from "@mui/material";

export default function SingleMemo() {
  return (
    <SK
      variant="rounded"
      width="100%"
      height="14rem"
      animation="wave"
      sx={{ borderRadius: "1rem" }}
    />
  );
}
